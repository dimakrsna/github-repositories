import { axiosInstance } from './index'
import { ApiTypes } from 'src/types/index'

// export default {
//   signIn: async () => {
//     return await axiosInstance.get(`/`).then(response => {
//       return response
//     }).catch(error => ({ error }))
//   },
// }